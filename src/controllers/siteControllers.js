const SiteController = {
    
    index(req, res) {
        res.send("home");
    },
};

//--------------------------------------------------------------------------------------
module.exports = SiteController;
